enum CustomTextFieldType {
  text,
  yearPicker,
  datePicker,
  dropdown,
}

enum AlertType {
  success,
  error,
  warning,
}

enum SortType {
  newest,
  oldest,
}

enum TextTransformType {
  normal,
  uppercase,
  lowercase,
  capitalize,
  capitalizeWords,
}
